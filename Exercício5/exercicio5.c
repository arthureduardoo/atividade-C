/*
 * O que é um vetor?
 * - Um vetor é usado para guardar vários valores do mesmo tipo.
 * - As posições do vetor são acessadas usando um índice, que começa no 0.
 * Exmplo de um vetor: 
 int numeros[4] = { 20, 25, 30, 30};
printf("%d\n", numeros[0]); // Exibe: 20

 * O que é uma string?
 * - Uma string é um vetor de caracteres usado para representar texto.
 * - Ela termina com um caractere especial '\0' que marca o fim da string.
 *Exemplo de uma string:
 char nome[5] = "Bruno";
printf("%s\n", nome); // Exibe: Bruno

 */
